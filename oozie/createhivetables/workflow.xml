<workflow-app name="CREATEHIVESCHEMA" xmlns="uri:oozie:workflow:0.4">
    <!-- Start job control node -->
    <start to="hive-create" />
    <!-- Hive table creation -->
    <action name="hive-create">
        <hive xmlns="uri:oozie:hive-action:0.4">
            <job-tracker>${job_tracker}</job-tracker>
            <name-node>${name_node}</name-node>
            <!-- Set up connection to the actual Hive Metastore -->
            <configuration>
                <property>
                    <name>hive.metastore.uris</name>
                    <value>thrift://localhost:9083</value>
                </property>
            </configuration>
            <script>${scripts_location}/CREATESTARHIVE.sql</script>
        </hive>

        <ok to="end" />
        <error to="Kill" />
    </action>

    <!-- Kill job control node -->
   <kill name="Kill">
       <message>${wf:errorMessage(wf:lastErrorNode())}</message>
   </kill>

    <!-- End job control node -->
    <end name="end" />
</workflow-app>
